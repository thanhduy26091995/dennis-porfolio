services:
  app:
    build: .
    container_name: dennis-portfolio
    restart: unless-stopped
    # Bind to localhost only — the host's system nginx reverse-proxies to this.
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NODE_ENV=production
